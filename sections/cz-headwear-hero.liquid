{% if collection.handle == 'headwear' %}
{{ 'section-cz-headwear-hero.css' | asset_url | stylesheet_tag }}

<section class="cz-headwear-hero color-{{ section.settings.color_scheme }} gradient" aria-labelledby="CzHeadwearTitle-{{ section.id }}">
  <div class="cz-headwear-hero__media" aria-hidden="true">
    <img
      src="{{ 'cz-headwear-hero-v1.jpg' | asset_url }}"
      alt=""
      width="1536"
      height="864"
      loading="eager"
      fetchpriority="high"
    >
  </div>
  <div class="cz-headwear-hero__shade"></div>
  <div class="cz-headwear-hero__content page-width">
    {% if section.settings.eyebrow != blank %}
      <p class="cz-headwear-hero__eyebrow">{{ section.settings.eyebrow | escape }}</p>
    {% endif %}
    <h1 id="CzHeadwearTitle-{{ section.id }}" class="cz-headwear-hero__title">
      {{ collection.title | escape }}
    </h1>
    {% if section.settings.copy != blank %}
      <div class="cz-headwear-hero__copy rte">{{ section.settings.copy }}</div>
    {% elsif collection.description != blank %}
      <div class="cz-headwear-hero__copy rte">{{ collection.description }}</div>
    {% endif %}
    <a class="cz-headwear-hero__button button button--primary" href="#HeadwearProductGrid">
      {{ section.settings.button_label | escape }}
      <span aria-hidden="true">→</span>
    </a>
  </div>
  {%- assign headwear_note_words = section.settings.note | split: ' ' -%}
  <p class="cz-headwear-hero__note" aria-hidden="true">
    {%- for word in headwear_note_words -%}
      {%- if forloop.last -%}<span>{{ word | escape }}</span>{%- else -%}{{ word | escape }} {% endif -%}
    {%- endfor -%}
  </p>
</section>
{% endif %}

{% schema %}
{
  "name": "Headwear hero",
  "tag": "section",
  "class": "cz-headwear-hero-section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "CROWN YOUR INSTINCT" },
    { "type": "richtext", "id": "copy", "label": "Description", "default": "<p>Performance-built headwear for people who refuse to blend in.</p>" },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "SHOP HEADWEAR" },
    { "type": "text", "id": "note", "label": "Side note", "default": "CREATIVITY CANNOT BE CAGED." },
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-1" }
  ],
  "presets": [{ "name": "Headwear hero" }]
}
{% endschema %}
